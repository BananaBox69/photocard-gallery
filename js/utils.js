// Remove any 'export' or 'import' statements
var MEMBER_COLORS = {
  'irene': '#F8C8DC', 
  'seulgi': '#FFFACD',
  // ... rest of your colors
};

var GROUP_ABBR = { 'Red Velvet': 'RV', 'IU': 'IU', 'aespa': 'AE' };
// ... rest of your constants

// Make functions global
window.darkenColor = function(hex, percent) {
  // ... implementation ...
};

window.calculateDiscountedPrice = function(price, type) {
  // ... implementation ...
};
