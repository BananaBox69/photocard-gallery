var THEMES = {
  'Red Velvet': { primary: '#F05252', secondary: '#F87171', initial: 'RV' },
  // ... other themes
};

var currentBias = null;

window.initThemes = function() {
  // ... implementation ...
};

window.applyTheme = function(name) {
  // ... implementation ...
};

window.getCurrentBias = function() {
  return currentBias;
};
